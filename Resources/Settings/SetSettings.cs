using System;
using System.Collections.Generic;
using System.Text;

using FifthBot.Resources.Datatypes;

namespace FifthBot.Resources.Settings
{
    public static class SetSettings
    {
        /*
        static List<ServerSetting> serverSettings;

        static SetSettings()
        {
            serverSettings = new List<ServerSetting>();
        }

        public static void set(ServerSetting s)
        {
            serverSettings.Add(s);
        }
        public static ServerSetting get(ulong id)
        {
            return serverSettings.Find(x => x.serverID == id);
        }
        */


    }
}
